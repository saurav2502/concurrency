spring:
  application:
    name: currency-exchange-service-dev
  cloud:
    config:
      uri: http://localhost:8888/
  profiles:
    active: dev
    label: developement

server:
  address: localhost

